import React from 'react';

const Footer = () => {
  return (
    <footer
      className="app--container-footer"
      style={{ position: 'fixed', bottom: '0', zIndex: '8000' }}
    ></footer>
  );
};

export default Footer;
